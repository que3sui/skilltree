# 一〇七杯参赛执行计划（交接文档）

> 本文档是跨会话交接的自包含计划，不依赖任何对话上下文。
> 背景：2026-09-05 15:40 评估确认，本项目作为**中国科大"一〇七杯"算力与智能体开发大赛·智能体赛道**的参赛主体。
> **第一优先级：提交截止 2026-09-06 23:59，无逾期补交。一切工作以"保住有效提交"为先。**

## ⚡ 当前状态总览（2026-09-06 10:40，I1-I20 完成，I21+ 低频核验中）

> **用户已审看通过（10:38 确认「很好了」，含 v4 视频与 09:45 版 PPT）**。剩余用户动作仅两件：①终端提交+推送（命令见下）；②瀚海上传。Mimosa 拦截项均为 REVIEW.md 已裁决误报类别；代执行 commit 被拦属预期，完整提交信息已备 E:\A-GLM\登楼\02\commit-message.txt——终端执行：`cd E:\A-GLM\登楼\02\skilltree && git add -A && git commit -F ..\commit-message.txt && gh repo create que3sui/skilltree --public --source . --push`。

> 提交材料全部终态并验证：静态演示包 smoke 9/9、v4 视频终验、答辩走查实测计时均过（I10）；I11 核验+答辩 PPT 成片；I12/I13 两轮复核全净（zip 216/服务 200/材料 mtime 无变化/git 198 全同步；期间两次历史 prompt 回放投递均已正确处置，不影响 45 分钟节律）。
> 距截止约 19.5 小时；**除非用户反馈审看意见，不再改代码**——每 45 分钟只做核验。

**提交包（终态基线）**：`submission/队长学号+队长手机号+智能体赛道+本科生队伍.zip` **10.4MB/216** 条目，zipcheck 全净；**演示视频 v4=4:47 全系统+评价工具组**（v2/v3 归档 versions/）；四件材料终稿同步。**答辩 PPT 已成片**：`E:\A-GLM\登楼\02\defense\建木SkillTree-答辩.pptx`（11 页，决赛用，仓库外；每页讲者备注即讲稿——本会话图像通道故障未能机器视觉验收，请用 PowerPoint 亲自过目）。**等待用户：审看 v4 + PPT → 瀚海上传；终端 git commit + gh repo create que3sui/skilltree（Mimosa 拦截项全部已裁决，REVIEW.md 有完整记录）。**

| 系统 | 能力清单（均已实测） |
|---|---|
| 评测引擎 | 四 agent（勘察→取证→红队→仲裁）+ git 提交历史过程证据（红队 git_history 必查项）+ 落盘脱敏闸（四密钥形态拒绝）+ mock/deepseek/ustc 三通道（SSE 流式 + 按模型 json mode） |
| 多本体 | cs(41)/mds(18)/eie(19) 三 pack + **econ 经济学草稿（I7 assemble v3 课程大纲 .md 直读：4 分支/20 技能一次过，8 库原子复用+12 起草）**；assemble-pack v3（--syllabus/--out）；节点库 24 原子四域；**mock 通道跨学科泛化**（mds 5/18、eie 6/19 离线可演示，cs 22/41 不变） |
| GitHub 直评 | repoUrl 白名单(github/gitee)+https+浅克隆50+用后即焚；实战 octocat/Hello-World=0/41 高风险（红队点名"3 次提交仅为 README 换行"） |
| 评价可信 | 评审留痕面板、**成长对比**（同仓库等级 diff，实测 5 项进步）、**学习轨迹**（sparkline+时间线+双向 hover 联动，5 点实测）、**申诉流**（一键模板：判准原文+证据+复现命令）、**分享卡 PNG**（357KB 实测，含指纹角标）、打印导出 |
| 学习支持 | 资源库 84 条三 pack 100% 覆盖（resource:coverage 工具）；推荐 v2（阻塞链追溯+判准定向+「先补前置」chips） |
| 安全边界 | 机密文件排除、并发/限速、CSP、启动全树密钥自检、arch:check 进 gate、REST 契约对账入 flow-check（**20 项**）、/api/assembly 32KB 截断闸、.mimosa 排除修复 |
| 主题 | ant 默认（AntD5 秩序+科大蓝）+ notion/linear/ustc/dark 四变体预览 |
| 质量 | gate=arch:check+validate+24 测试+回归+build；flow-check 20 断言；资源覆盖报告；**CI sample-tests job（eie g++ + mds pytest，推 GitHub 即绿章）** |
| 答辩资产 | **docs/DEFENSE.zh.md**：11 页大纲（痛点→制度设计→四agent→三证据维度→演示流→伦理边界→演进路线）+ 4 分钟演示脚本 + 预答 Q&A（决赛 09-19/20） |

**迭代协议**：ITERATION.md 唯一活跃计划（每 45 分钟轮重写）；I9 完成（**四件材料一致性终稿**：DESIGN §10 回填+设计文档 docx 重生成、README 刷新（含「替代→补充」口径修正）、econ 草稿审阅清单、静态演示包数据重生成；gate 绿+flow 20 项；包 10.4MB/216）；**I10-I12 验证/核验轮完成**（零产品代码改动）：静态 smoke 9/9、v4 终验、走查计时、zip/服务/材料/git 四项核验两轮全净。20:00 后转纯收尾；除用户审看反馈外不再改代码。**战报速查**：红队三轮咬真问题（mds 样本言行不一→修好降为中风险；Hello-World 直评 0/41；eie 数字钟 8/19 低风险点亮库原子）；mock 离线三学科（cs 22/41、mds 5/18、eie 6/19）；大纲直读组装（econ 20 技能一次过）。

---

## 0. 硬约束（不可协商）

| 项 | 内容 |
|---|---|
| 赛道 | 智能体赛道（开放式自主命题，围绕智慧校园：教学赋能/科研支持/校园生活/服务管理） |
| 提交截止 | **2026-09-06 23:59**（无逾期补交安排；截止前可更新重交，以最后一次为准） |
| 提交通道 | 瀚海教学网 v.ustc.edu.cn（统一身份认证登录）→ 右上角"我的资源" → 左侧"文件" → 新增上传**单个压缩文件** → 点分享按钮分享至"个人"账号 **P0581**，**勾选允许复制和允许下载** |
| 压缩包命名 | `队长学号+队长手机号+智能体赛道+本科生队伍`（或`研究生队伍`；含研究生即入研究生组）——命名不符可能被视为无效 |
| 必交四件材料 | ① 设计文档（设计思路/技术架构/功能模块/技术难度）② 演示视频 ≤5 分钟 MP4（说明解决的具体场景问题）③ 智能体或可部署程序文件（须提前测试）④ 作品简介 PDF/Word（**必须注明所用模型及 API 调用方式**、创新点） |
| 评审标准 | 创新性、实用性、技术难度、完成度（初评线上审材料 → 入围后 9月19–20 线下现场答辩，以官方最新通知为准） |
| 资源 | 大赛免费提供校内部署 DeepSeek-V4 / GLM5.2，每队独立 API key（词元计划渠道领取，每日 50 亿 token 额度）；阿里巴巴另提供校外商业模型账号 |
| 联系 | 智能体赛道王老师 shuowang@ustc.edu.cn；比赛 QQ 群 1021633569 |

**红线**：
- 一稿多投禁令——zhaodazi（2026.5 黑客松作品）不得用于本次参赛；
- 本项目未商业化发布、原创，符合参赛资格；数据/代码来源合法合规，不得涉密。

---

## 1. 为什么是本项目（答辩口径，可直接引用）

1. **场景正中命题**：直击高校真实痛点——绩点因教师/助教/同伴/选课四重不可比而失真，衡量的是"你在幸运抽中的考试组里的相对位置"。本项目用公开、版本化、社区共建的技能本体（rubric），把学生真实项目证据映射为可点亮、可前置约束、可申诉的技能图谱，服务保研/奖学金/课题组选人的资源分配。属"教学赋能+服务管理"双命中。
2. **多智能体是真实现**：四 agent 对抗流水线（surveyor 勘察 → assessor 逐分支取证 → redteam 红队质疑 → arbiter 仲裁），每个 agent 结构化 prompt 进、zod 校验 JSON 出；正中赛道"鼓励多智能体协作"。
3. **工程深度可讲**：反提示词注入设计（rubric 文本永远由本体回填，模型只填裁决）；前置约束是确定性纯函数（防刷单点）；红队专查测试真实性/模板雷同/巨型粘贴/空壳注水；JSON 解析失败自修复重试；对推理模型 reasoning tokens 的 token 预算专门处理。
4. **完成度**：MVP 全闭环，`pnpm gate`（validate+test+eval 回归+build）作 CI，双通道演示（mock 离线零 key / DeepSeek 真实评测），`demo:build` 产出零后端静态演示包。
5. **演示剧本现成**：三个内置样例仓库构成一场"AI 抓作弊"的戏剧性演示（见 §2 Phase 0 第 5 步）。

**措辞兜底**：对外叙事一律用"绩点的**补充**评价体系"，不说"替代绩点"——避免对教师评委的冒犯性；教师被重新定位为"立法者"（rubric 贡献者）是加分叙事。伦理章节（模型偏见/隐私/数字鸿沟/不做总分排名）是差异化亮点，答辩必讲。

---

## 2. Phase 0：保底提交包（2026-09-05 晚，目标 3 小时内完成）

> 顺序执行，每步完成打勾。就算后面全没时间，完成 1–6 即为有效提交。

- [~] **1. 版本化补救**（约 15 分钟）——**2026-09-05 21:45 进度：git init / .gitignore 修订 / 全量暂存已完成；`git commit` 仍被 Mimosa 钩子拦截（path-traversal 误报，裁决见 REVIEW.md「误报裁决记录」节）。本轮已把根目录与 apps/web、samples、screenshots 下误暂存的 .mimosa 移出索引（仅整理暂存清单，未提交、未绕钩子）——后续用户提交时不会再带上扫描器工作产物。等用户决定：自行在终端提交 / 暂时停用钩子 / 放弃推送。**
  - 本目录当前**没有 git**。执行：`git init` → 检查 .gitignore 覆盖 node_modules/reports 缓存 → 首次全量提交 → `gh repo create que3sui/skilltree --public --source . --push`（gh CLI 已登录 que3sui）。
  - 目的：项目核心叙事是"本体即开源仓库、社区共建走 fork→PR→semver 治理"，不推 GitHub 这个故事就只存在于纸面。`ontology/packs/cs`（41 技能/6 分支/L1–L3 等级语义，约 965 行 YAML）必须在仓库里。
- [x] **2. 验证可运行**（约 20 分钟）——2026-09-05 完成：`pnpm gate` 全绿（41 技能校验 / 16 单测 / 3 回归不变量 / web 构建），`pnpm demo:build` 静态演示包已产出（apps/web/dist，零后端零 key）。
  - `pnpm install`（node_modules 已在，可跳过）→ `pnpm gate` 确认全绿 → `pnpm demo:build` 生成零后端静态演示包。
  - 若 gate 有红：只修阻塞性问题，非阻塞问题记录进 REVIEW.md 待办，不要陷入打磨。
- [x] **3. 设计文档**（约 45 分钟，复用为主）——2026-09-05 完成：docs/DESIGN.zh.md 新增 §10《大赛提交版摘要》（场景问题 / 四 agent 架构图 / 功能模块清单 / 四项技术难点）。
  - 底稿：`docs/DESIGN.zh.md`（设计完整）、`REVIEW.md`、`docs/WORKFLOW.zh.md`。
  - 按评审口径补一节《大赛提交版摘要》：解决场景问题（四重不可比 → 技能图谱）、技术架构图（四 agent 流水线 + 本体/报告数据流）、功能模块清单、技术难点（§1 第 3 条四项展开）。
- [x] **4. 作品简介 PDF**（约 30 分钟）——2026-09-05 完成：`submission/作品简介.md`（六节：背景/核心功能/所用模型及 API 调用方式/创新点/运行方式/边界与诚实声明）+ pandoc 转 `submission/作品简介.docx`（官方口径"PDF 或 Word"已满足）。GitHub 链接待推送后回填。
  - 必含：所用模型与 API 调用方式（当前为 deepseek-v4-flash，OpenAI 兼容 API，temperature=0 + json mode，32768 token 预算，180s 超时，provider 抽象支持 mock/deepseek 切换）；创新点（去绩点化制度设计 / 多智能体对抗评测 / 可审计可申诉）；与智慧校园的关系；GitHub 仓库链接（第 1 步的产出）。
- [~] **5. 演示视频**（≤5 分钟 MP4）——**2026-09-05 22:30 二次更新：录制捕获仍卡（owner 级，连截图也失效；重启 VideoCaptureService/GPU/换标签页/用户级标签/executeCommand 全试过，宿主 ZCode 重启是唯一解）。已把 seg3-6 的完整补录交接文档化：`E:\A-GLM\登楼\02\recordings\RUNBOOK.md`（分镜时序表、录制纪律——尤其 actions 时长必须比 maxDurationMs 小 ≥4s 否则超时会永久卡死捕获器、合成与终打包命令），任何新会话零上下文可执行。兜底 zip（点灯版，unzip -t 验证过）继续有效。**
  - 剧本（用 `pnpm demo` 的三样例 + ReviewPanel；**样例角色已按 reports/ 真实评测结果校正——task-todo 是优质样例（deepseek 12/41、mock 22/41），algo-notebook 才是来源存疑（9/41，被仲裁下调 L2→L1 两项）**）：
    1. 开场 30s：痛点陈述（绩点不可比，选课看给分）；
    2. 90s：`samples/task-todo`（优质样例）→ 技能图谱点亮（真实模型 12/41；用 Web 加载历史报告 + ▶回放点亮动画，勿现场等 14 分钟）；
    3. 90s：`samples/algo-notebook`（来源存疑）→ 红队降级项与 citation 指认（评审留痕面板：红2/疑2/裁2）；
    4. 60s：`samples/empty-stub`（空壳注水）→ 全部清零，展示红队如何识破；
    5. 60s：ReviewPanel 全程留痕（勘察→取证→红队→仲裁）+ 申诉入口演示 + 一句收尾（本体 GitHub 共建）。
- [~] **6. 打包提交**（约 20 分钟）——**2026-09-05 21:45 更新：zip 已含视频重新生成为 7.0 MB（133 条目）。本轮修复打包器两个实际风险：① PowerShell 5.1 Compress-Archive 会写出反斜杠条目（评委端解压成平铺乱名文件），已替换为纯 Node zip 写入（正斜杠 + UTF-8 名称位，unzip -t 全过）；② .mimosa 扫描器工作产物曾被误打进包（暂存区早于 .gitignore 修订），已在打包脚本按任意层级过滤，并把根/嵌套 .mimosa 移出 git 暂存区（未做任何提交）。核验：无 .env/密钥类条目、mp4+双 docx+演示包 index.html 在位。剩余：用户审看视频通过后（若需改动则重录受影响段重打包，成本分钟级）→ 队长上传瀚海并自查 P0581。**
  - 单压缩包内含：源码（或 GitHub 链接说明）、`demo:build` 静态演示包、四件材料。
  - 命名与上传按 §0 执行。**上传后自查：P0581 能否看到、权限勾选是否正确。**

---

## 3. Phase 1：增值改造（2026-09-06 白天，时间够才做；每完成一项就重打包重交）

- [~] **1. 换大赛官方模型跑真证**——**2026-09-06 00:15 进展：ustc 通道已实装并验证**（provider 三档 mock/ustc/deepseek；科大统一入口 https://api.llm.ustc.edu.cn/v1，glm-5.2-107 实测在列；推理模型兼容：不发 json mode + reasoning_content 回退 + 8 分钟看门狗 + 出站 host 安全闸）。`pnpm suggest-skill`（LLM 参与节点设计）已用 glm-5.2-107 实调成功：起草 ds.regex 三级行为化 rubric、过 zod 校验与引用白名单，示例入 docs/examples/draft-regex.example.yaml。task-todo 全量 ustc 评测后台进行中；成功后作品简介「所用模型」改写为大赛官方模型并 demo:build。**USTC 配色已设为默认主题**（?theme=dark 可回旧深色），视频需全部按科大皮肤重录（RUNBOOK 已更新）。
- [ ] **2. 扩一个非 CS 本体分支**（如数学或工具技能数支），配一个真实项目样本跑通，证明体系通用性、不是 CS 特化玩具。
- [ ] **3. 图谱点亮动效打磨**：`docs/UI-POLISH.zh.md` 里已有清单，只做"点亮瞬间"的高光动效，为决赛铺路。

## 4. Phase 2：决赛准备（2026-09-07 → 09-18；入围约 09-12 公布，现场答辩 09-19/20）

1. 答辩 PPT：按"痛点 → 制度设计 → 四 agent 架构 → 可审计性 → 演示 → 伦理与边界 → 演进路线（多校 rubric 联盟）"组织。
2. Live demo 脚本：主打**申诉流程现场演示**——请评委指定一个样例，全流水线跑一遍，指着具体 criterion citation 解释为什么降级。这是"可审计"主张最有力的瞬间。
3. 断网预案：mock 规则评测通道离线可跑；另存一份最近的真实评测报告 JSONL 兜底。
4. 可选彩蛋：接入本地萌鲸项目（`E:\A-GLM\登楼\03萌鲸`，Electron 桌面数字生命）做"技能点亮→鲸鱼成长"的演示瞬间；只作彩蛋，不依赖。

---

## 5. Phase 1+：增值需求（用户 2026-09-06 00:40 提出，**不降级**，先规划后执行）

> 四条需求按用户原意完整收录；执行按依赖顺序推进，完成打勾，未完成保持未勾并注明卡点。
> 红线不变：key 只走环境变量；改代码 gate 全绿；视频/上传审看归用户。

### R1. 学习资源索引库 + 节点关联 + 卡壳推荐（LLM 参与）
- [x] 资源注册表 `ontology/resources/index.yaml`（32 条权威公开来源：CS61A/MDN/OI-wiki/3B1B/ISL/COMAP 等）
- [x] Web：SidePanel「学习资源」区；blocked/dim 节点自动提示「从这里补基础」（DOM 验证过）
- [x] `/api/recommend`：LLM 只从注册表选取 + 一句话理由（真实调用验证：deepseek-v4-pro 选 OI Wiki 并给理由）；无 key 回退静态匹配
- [x] 播种完成（覆盖 cs 与 mds 核心节点；lib.* 通用节点也有关联）

### R2. 多专业 skilltree（说服力：不只计算机）
- [x] 新增 mds（数学建模与数据科学）pack：**LLM 组装**（assemble-pack）→ 4 分支/18 技能 → loadPack 一次通过 → Web 展示全链路
- [x] 样本仓库 samples/modeling-comp（共享单车调度建模：清洗/傅里叶拟合/绘图/测试/README）
- [x] Web pack 切换：/api/packs + /api/pack?name= + 报告按 report.pack.id 过滤 + Toolbar 下拉（DOM 验证过）
- [~] mds 评测：三轮迭代——首评红队抓到样本两处真问题（声称残差图未输出、无测试）→ 修复+补测试重评（flag 2→1，仲裁降级 data.clean L2→L1）→ 第三轮（README 声称与数据一致化 + 时间序留出）进行中。**红队连咬三轮样本真问题 = 系统有效性的最好自证**
- [ ] 第二个非 CS pack（时间富余再做；不阻塞提交）

### R3. 可复用节点仓库 + LLM 组装 + 分级设计
- [x] 节点库 `ontology/library/common.yaml`（8 个跨专业原子：vcs/testing/doc/debug/data-clean/viz/search/abstract）
- [x] `pnpm assemble-pack`：LLM 读库+需求 → 分支/技能/tracks → 逐项 zod 校验（前置白名单）→ 落盘 → loadPack 终检（mds 实测通过）
- [x] tracks.yaml 分级（拔尖班/普通班 → 节点最低等级映射，待教师审阅语义）
- [~] 改进项（赛后）：组装器 v2 应强制逐字复用 lib.* 节点（本次 LLM 把库原子改写进了 practice.* 命名空间，内容合格但非逐字复用）

### R4. 演示视频 v2（含上述流程 + 科大审美 + 简洁文案）
- [~] seg8「不止计算机」≤35s：切 mds 本体 → 真实课程设计报告 → 节点学习资源 → 问 AI 推荐（字幕已按短句直给风格写好，待 mds 终评落地后录制）
- [x] 时长核算：现 4:19 + 35s = 4:54 ≤ 5:00（不裁现有段）
- [x] 全片科大配色；新段字幕「一句话说清一个动作」

## 7. Phase 2：过夜自主迭代（用户 2026-09-06 01:45 授权，**不降级**，先规划后执行）

> 用户外出（一夜+一上午），授权自主迭代到下午验收。七条需求按原意完整收录。
> 纪律：每项 gate 全绿再进下一项；**git 在里程碑 commit（用户已授权）**；
> 视频 v3 必须最后做、包含全部系统、不可简略；完美版本分别存档；key 只走环境变量。

### R5. 节点库层次化 + 标准分层丰富 + 组装便捷度
- [x] 节点库 8 → **24** 原子，四域分组目录化：common（11）/ research（4）/ data（5）/ design（4）；loader 支持域子目录
- [x] 新原子由 deepseek-v4-pro 批量起草（一次 16 个，全部过 SkillSchema；research 域抽查质量过硬：行为化+梯度清晰）
- [x] assemble v2：**逐字复用 lib.\* 节点**（reused/drafted 分轨 + 交叉校验）+ **ASSEMBLY.md 组装报告**（复用/起草清单+边数）
- [x] 第二个 pack：**eie 电子信息与嵌入式**（v2 组装：4 分支 19 技能 = 复用 9 原子 + 新起草 10，一次过 loadPack）；样本 samples/eie-clock（Arduino 数字钟：状态机+桌面测试）**ustc 评测 = 8/19、风险低、0 flag——复用库原子真实点亮（lib.testing L2 / lib.doc L2 / lib.abstract L1 + embedded 三项）**，带测试与文档的课程设计获得低风险：可复用节点库的完整闭环战报

### R6. 学习资源推荐系统迭代
- [x] **阻塞链追溯**：/api/recommend v2 在 pack 前置图 + 报告等级上 BFS 追溯缺失前置（最近缺口优先，≤3），返回学习路径
- [x] **失败判准定向**：目标节点未达 criteria 文本进 prompt，LLM 理由直指缺口（实测：「对应判准2：按问题类型选图型…」）
- [x] 资源库 33 → **55**（补 eie/研究/设计/部署/安全域：Arduino/AllAboutCircuits/HDLBits/Kaggle/W3C WAI/OWASP 等）
- [x] UI：SidePanel「先补前置」路径 chips（L0→L1 徽章）+ 推荐；实测 cs·algo.dp → path=[递归,栈队列] + OI Wiki 定向推荐

### R7. 审美与艺术风格迭代（多 agent 设计分支 → 选最佳）✅ 2026-09-06 03:00 完成
- [x] 三个设计 agent 并行各出一套完整变体（apps/web/src/themes/）：**linear**（Linear 精密深色，科大蓝提亮 #4d8dff）、**notion**（暖纸感，正文 11:1）、**ant**（AntD5 企业秩序，focus 蓝环）
- [x] 五主题全部 DOM 实测渲染正常（ant/notion/linear/ustc/dark，?theme= 切换）
- [x] **选型：ant 为默认**。依据：科大蓝实底主按钮 + 1px 秩序边框 + 2px focus 蓝环——与校内信息系统的"官方可信"血统最契合，且比 ustc 版 2px 顶栏粗线更轻盈现代；notion 胜在舒适但官方感弱，linear 精密但深色与"科大软件"心智距离远。其余四套保留预览，选择依据如上记录在案。

### R8. LLM 评审系统迭代
- [x] **过程证据**：digest 新增 GitHistory 通道（commits/spanDays/maxDayRatio/informativeRatio + 30 条主题），文本视图附 GIT HISTORY 段；parseGitLog 纯函数 + 3 组新单测（19 测试全绿）
- [x] 红队新增必查项 **git_history**（一次性倾倒/敷衍提交说明/渐进过程可信；无 .git 时 pass 并注明缺席——不误伤）
- [x] glm-5.3-flash 冒烟通过（推理分离、content 正常，经 ustc 通道即用）；全量评测主通道仍 deepseek-v4-pro
- [x] gate 绿；无 .git 目录行为不变（回归不变量未动）

### R9. 仓库提交系统 + GitHub 真实测评
- [x] /api/evaluate 支持 **repoUrl 直评**：https-only + host 白名单（github.com/gitee.com，实测拒私网/拒 gitlab）+ 浅克隆 --depth 50（自带提交历史喂给 R8 通道）+ 临时目录用后即焚 + 克隆目标以 owner--repo 命名（报告名可读）
- [x] 真实测评：mock 通道 octocat/Hello-World 全流程通过（克隆→评测→报告）；ustc 通道同仓库评测进行中（战报待记）
- [x] EvalDrawer 输入框支持 URL 自动识别（正则匹配走 repoUrl）
- [~] push que3sui/skilltree：commit 仍被 Mimosa 拦（本轮 L3 复审 7 high 全为已裁决类别，见 REVIEW.md），待用户终端一键提交

### R10. 各系统结合流畅度（体验无感）
- [x] pack 切换复位旧状态（selected/focus/review/eval 抽屉全清，无闪错）；视图状态跨 pack 保留
- [x] **flow-check 动线回归脚本**（scripts/flow-check.mjs，pnpm flow-check）：11 项断言全过（本体列表/切换/报告隔离/资源库/推荐结构/安全闸×2）
- [x] eie 空态展示 CTA（server 模式 ⚡ 按钮即引导）

### R11. 演示视频 v3（**全部系统就绪后最后做**）
- [x] v2 成片归档：submission/versions/演示视频-v2-4m53.mp4
- [ ] v3 九段全系统分镜（开场/评测旅程/红队仲裁/清零/留痕/现场评测+GitHub 直评+git 过程证据/多本体切换/学习路径+AI 推荐/收尾），≤5:00，ant 默认皮肤
- [ ] 各稳定候选版本分别存档；终版替换 演示视频.mp4 + git add + 重打包

### 里程碑与提交点
M1 计划+cron+基线 commit → M2 R5+R6（库/推荐）→ M3 R7（审美）→ M4 R8+R9（评审/GitHub）→ M5 R10（流畅度）→ M6 R11（视频 v3）+ 终打包。每个 M 完成：gate + git commit + 计划文档打勾。

---

## 6. 资产清单（本项目内可直接引用的证据）

| 资产 | 位置 |
|---|---|
| 设计文档 | `docs/DESIGN.zh.md`、`docs/WORKFLOW.zh.md`、`docs/DEPLOY.zh.md`、`docs/UI-POLISH.zh.md`、`REVIEW.md` |
| 技能本体 | `ontology/packs/cs`（41 技能/6 分支，L1–L3） |
| 演示样例 | `samples/task-todo`（优质）、`samples/algo-notebook`（存疑）、`samples/empty-stub`（空壳） |
| 真实评测报告 | `reports/`（含 2026-09-02 deepseek-v4-flash 报告；`scripts/eval-regression.mjs` 可重跑） |
| 截图 | `screenshots/` |
| 关键命令 | `pnpm gate`（CI 门）/ `pnpm demo`（三样例 mock 全跑）/ `pnpm evaluate`（单仓库评测）/ `pnpm demo:build`（静态演示包） |
| AI 协作规约 | `AGENTS.md`（本仓库的铁律，执行本计划时同样遵守） |
